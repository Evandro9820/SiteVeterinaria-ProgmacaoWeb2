import Navbar from "../NavBar";
import Logo from "../../assets/logo2.png";
const Header = () => {
  return (
    <header>
      
      <Navbar />
      {/* Você pode adicionar outros elementos ao header, como um banner, logo, etc. */}
    </header>
  );
};

export default Header;
